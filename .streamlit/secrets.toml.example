# 이 파일을 복사해서 secrets.toml 로 이름을 바꾸고 실제 값을 입력하세요.
# secrets.toml 은 절대 GitHub에 올리지 마세요! (.gitignore에 추가)

# Supabase PostgreSQL 연결 문자열
# Supabase 대시보드 → Settings → Database → Connection string → URI 탭에서 복사
DATABASE_URL = "postgresql://postgres:[비밀번호]@db.[프로젝트ID].supabase.co:5432/postgres"
